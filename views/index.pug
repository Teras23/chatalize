extends layout

block content
  h1= title
  p Welcome to #{title}
  ol
    each chat in files
      li
        a(href="/chat/" + chat.fileName) #{chat.title}
        |  #{chat.messageCount}
        if chat.title !== chat.participants
          | - #{chat.participants}

